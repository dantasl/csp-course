channel a, b, c

A = {a, b, c}

P_A = a -> b -> c -> SKIP

PA_RUN = P_A [| A |] RUN(A)

-- Podemos simplificar PA_RUN para simplesmente P_A, pois a atual composição não opera nenhuma restrição a P_A.

assert P_A [T= PA_PAR_RUN